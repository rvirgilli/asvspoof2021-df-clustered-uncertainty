# EXP-107 v2 public preregistration placeholder — stopped

The pre-execution adversarial audit returned NO-GO before implementation or
compute. It found an estimand mismatch at ties/jumps, uncorrected cell-component
inflation in the raw product bootstrap, and degenerate pointwise scaling in the
rare-error tail. No v2 output exists. The full stopped record and audit will be
released with the successor v3 contract; the stopped v1 record remains available
separately.
