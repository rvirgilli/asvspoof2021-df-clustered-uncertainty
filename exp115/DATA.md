# EXP-115 data record

EXP-115 creates no new scores and copies no licensed SpoofCeleb data into the
repository. It reads the exact manifest and score tables authenticated by the
EXP-114 provenance rerun.

Canonical durable archive:

`~/data/corpora/anti-spoofing/spoofceleb/derived/EXP-114-m1-spoofceleb-confirmation/EXP-114-provenance-rerun-20260830.tar`

Archive SHA-256:

`f5684da2dd52fcb31015a37ac62801ae20713da49a714f68f6cbcb57920cc697`

The input file hashes and complete census are frozen in `PREREG.md` and
enforced by `analyze.py`. Only derived aggregate JSON, small bootstrap EER
arrays, hashes, and documentation may enter the academic or public repository.
