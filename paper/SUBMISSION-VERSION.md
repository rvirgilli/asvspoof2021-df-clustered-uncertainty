# FIX4 submission

Artifact commit: `9a7e6ae2c37c6ae30ef72122184f5b0159a64703`.

The artifact was committed first, then its hash was inserted in the Artifact
paragraph and `main.pdf` rebuilt. That hash is the only manuscript-source change
between artifact and submission. The artifact-stage PDF is identified history
from `6f54787`; this submission contains the new rendering.

FIX4 applies the four P2 repairs from AUDIT-REVISE and adds public score provenance
and agreement at reported precision. All 303 obligation IDs remain; all five tables,
experimental results, supplements and references are unchanged. A full ranked
influence plot was omitted because only the top-five shares, not all 93 deletion
shares, are present in the allowed evidence.

The exact PDF checks and both guard outputs are in `fix4-validation/`.
The language-model disclosure and ethics statement stay on page four;
page five contains references only. No push, tag movement or amended commit.
The external `FIX4-NOTE.md` records both final commit hashes and full gate details.
