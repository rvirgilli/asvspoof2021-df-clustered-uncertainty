# Concentration rewrite: artifact stage

The current source implements the concentration rewrite; `main.pdf` is the
inherited audited five-page PDF from `8276097`, not a rendering of this source.
After this artifact is committed, the submission stage will insert that commit's
full hash in the Artifact paragraph and rebuild the PDF. No push or tag change
is part of this work.
