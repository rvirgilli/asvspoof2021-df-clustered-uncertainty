# Concentration rewrite submission

The current receipt is `RELEASE-RECEIPT-REWRITE-FINAL.json`, following
`RELEASE-RECEIPT-REWRITE-ARTIFACT.json`. `MANIFEST.json` binds the release payload.
The manuscript cites artifact `260e1e15e1ac6f79d4a7b083f5a51716dc8d7516`.

`rewrite-validation/pdf-audit.json` checks exactly five pages, references only on
page five, acknowledgment and ethics on page four, actual minimum font size,
Type 3 fonts, overfull boxes, undefined citations/references, every printed table
row and the intact repository URL. Both guard outputs are in the same directory.
`OBLIGATION-REWRITE.json` retains all predecessor and original obligations.

The new commits are local. Do not submit the artifact-stage predecessor PDF.
The fresh external audit and calibrated PDF-first review remain subsequent gates;
the cited new artifact must become publicly reachable before an anonymous
artifact audit. This rewrite does not push, move tags or upload a submission.
